'use strict';

describe('myApp.print module', function() {

  beforeEach(module('myApp.print'));

  describe('print controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var printCtrl = $controller('PrintCtrl');
      expect(printCtrl).toBeDefined();
    }));

  });
});